'use client';

import TestimonialsPage from '../page';

export default function ReviewsPage() {
  // We can pre-filter here if needed, but for now just show the main page
  return <TestimonialsPage />;
}
