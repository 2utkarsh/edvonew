# URL of the Laravel backend — no trailing slash
# On cPanel this is typically your api subdomain
BACKEND_URL=https://api.yourdomain.com

# Optional: override the port Next.js listens on (default: 3000)
# PORT=3000
